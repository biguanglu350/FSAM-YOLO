# Reef dataset config for FSAM-YOLO
# Path is resolved at runtime by the training script.
# This file only defines the dataset structure.

path: datasets/reef
train: images/train
val: images/val

# Classes
names:
  0: reef
